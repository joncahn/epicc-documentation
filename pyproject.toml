[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "epicc"
authors = [{name = "Jonathan Cahn", email = "jonathan.cahn at inrae.fr"}]
dynamic = ["version", "description"]
